#include <iostream>
#include <ifstream>
#include <string>

using namespace std;

// int countUnique(ifstream & input)
// {
//     string word;        //Similar to a linkedlist of strings, also has a size field
//     LinkStringSet set; // LinkStringSet is a Tapestry class

//     while (input >> word)
//     {
//         set.insert(word); // Skips the insertion if the word already exists
//     }

//     return set.size();
// }

int main()
{

    return 0;
}
